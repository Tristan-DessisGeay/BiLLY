import 'package:flutter/material.dart';

Widget red_point = Container(
  width: 10,
  height: 10,
  decoration: BoxDecoration(
    color: Colors.red,
    borderRadius: BorderRadius.all(
      Radius.circular(5)
    )
  ),
);  

Widget green_point = Container(
  width: 10,
  height: 10,
  decoration: BoxDecoration(
    color: Colors.green,
    borderRadius: BorderRadius.all(
      Radius.circular(5)
    )
  ),
);  

Widget orange_point = Container(
  width: 10,
  height: 10,
  decoration: BoxDecoration(
    color: Colors.orange,
    borderRadius: BorderRadius.all(
      Radius.circular(5)
    )
  ),
);  